# Veeraaj B Juneja — Portfolio (Local Preview)

Open `index.html` in your browser to view the portfolio. For a local server (recommended for proper routing), run:

```bash
# Python 3
python -m http.server 8000

# then open http://localhost:8000
```

Files added:
- `index.html` — main page
- `styles.css` — styling
- `script.js` — small interactions
- `resume.md` — text version of resume

Next steps: customize content (projects, contact email, resume) and optionally deploy to GitHub Pages.
